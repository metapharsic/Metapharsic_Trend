# Dependencies
node_modules/
**/node_modules/

# Next.js build output
.next/
**/.next/
out/

# Env files — secrets, DB creds, JWT keys. Never commit.
.env
.env.*
!.env.example

# Real data — DB dumps, backups. Never commit customer data.
Backup_f_B/
**/*.dump
**/*.sql

# Logs
logs/
**/*.log

# Expo / mobile build junk
mobile/.expo/
mobile/node_modules/

# OS/editor junk
.DS_Store
Thumbs.db
*.tsbuildinfo

# Uploaded runtime files (logos etc — regenerate per-deployment)
web/public/uploads/
!web/public/uploads/company/logo.png

# SSH keys and deployment credentials
.ssh_keys/
*.ppk

# Temporary exports and scratch
accounts_module_export.zip
accounts_module_export/
accounts_module_export_unzipped/
_to_delete/
web/reports-output/
brain/
.claude/


